# Validacao

Execute:

```powershell
.\scripts\test.ps1
```

Com o Edge CDP aberto e autenticado, valide uma coleta real:

```powershell
.\scripts\monitor_open_tab_once.ps1
```

Depois inicie o painel e abra `http://127.0.0.1:8088` para confirmar que o
status e os dois limites sao exibidos.
